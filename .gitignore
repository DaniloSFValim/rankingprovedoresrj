node_modules/
dist/
.next/
*.tsbuildinfo
.DS_Store
.env
.env.local

# Dados brutos da Anatel (grandes, reprodutiveis via ETL)
data/raw/
data/work/
data/*.sqlite
data/*.sqlite-*

# apps/web/public/data/ NAO e ignorado de proposito: sao os artefatos
# analiticos gerados pelo ETL, versionados para que `npm run build` funcione
# num clone limpo. apps/web/out/ (site compilado) e ignorado em apps/web: o
# workflow Deploy sempre recompila a partir de public/data.

# Test artifacts
test-results/
playwright-report/
